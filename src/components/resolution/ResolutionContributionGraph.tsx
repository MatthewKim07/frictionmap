/**
 * Resolution contribution graph — re-exports the heatmap and related pieces
 * for a single import path (Insights full-year view composes these directly).
 */
export { ResolutionHeatmap } from "@/components/resolution/ResolutionHeatmap";
export { ResolutionYearSelector } from "@/components/resolution/ResolutionYearSelector";
export { ResolutionLegend } from "@/components/resolution/ResolutionLegend";
